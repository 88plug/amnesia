# amnesia plugin-development repo

# Editor / OS
.DS_Store
*.swp
*~
.vscode/
.idea/

# Local-only test data the plugin produces in its CLAUDE_PLUGIN_DATA dir;
# never committed.
/scratch/
/tmp/
*.log

# Python helper bytecode
__pycache__/
*.pyc

# real eval/corpus data — never commit
eval_real_corpus.json
.real_corpus_manifest.json

# round-specific campaign sweep scripts (one-time artifacts)
scripts/sweep_r*.py

# eval outputs
eval_run_*.json
EVAL_REPORT.md
